name: in_app_messaging_drift
resolution: workspace
description: In-App Messaging persistence layer with Drift
version: 0.0.23
repository: https://github.com/rIIh/in_app_messaging

environment:
  sdk: ^3.5.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # framework
  in_app_messaging: ^0.0.22

  # utility
  logging: ^1.0.0
  path_provider: ^2.1.3
  drift: ^2.23.1
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # tests
  test: ^1.25.2
  checks: ^0.3.0

  # lints
  lints: ^6.0.0

  # codegen
  build_runner: ^2.4.11
  drift_dev: ^2.23.1
